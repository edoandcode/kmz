spring:

  application:
    name: kmz-backend-dev

  datasource:
    url: jdbc:h2:mem:kmz_db
    driverClassName: org.h2.Driver
    username: edoandcode
    password: 2025_kmz_2025

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  jwt:
    secret: ${JWT_SECRET}



