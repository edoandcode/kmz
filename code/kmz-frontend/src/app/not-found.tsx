import clsx from 'clsx';

const NotFound = () => {
    return (
        <div className={clsx(
            "w-full h-screen flex items-center justify-center"
        )}>
            <h1>404 - NotFound</h1>
        </div>
    )
}

export default NotFound